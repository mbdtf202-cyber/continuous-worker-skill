interface:
  display_name: "Continuous Worker"
  short_description: "Run recoverable 24/7 goal-oriented background work"
  default_prompt: "Use $continuous-worker to confirm a long-running task, start it safely, and keep reporting progress with a visual bar."

policy:
  allow_implicit_invocation: false
